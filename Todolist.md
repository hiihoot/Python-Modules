import sys
import os
import site


# sys module:
its a built in module comes with python, lets us talk to interpreter interpreter ibterpreter and access system-specific details

# os module:
its also a built in module, it lets us interact with the system like working with files and directories, managing paths accessing system information

# site module:
site module runs automatically at the start to config the package installed path, without site python would not know where installed packages is

os.path.basename(sys.prefix)


site.getsitepackages()[0]
looks for where packages installed


# import importlib
so import module is a built in module comes with python 

requests


import matplotlib.pyplot as plt
import numpy as np
import pandas as pd
